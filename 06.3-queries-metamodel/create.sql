CREATE TABLE ADDRESS (ID INTEGER NOT NULL, CITY VARCHAR, STATE VARCHAR, STREET VARCHAR, ZIP VARCHAR, PRIMARY KEY (ID))
CREATE TABLE DEPARTMENT (ID INTEGER NOT NULL, NAME2 VARCHAR, PRIMARY KEY (ID))
CREATE TABLE PROJECT (ID INTEGER NOT NULL, DTYPE VARCHAR(31), NAME VARCHAR, PRIMARY KEY (ID))
CREATE TABLE EMPLOYEE (ID INTEGER NOT NULL, NAME VARCHAR, SALARY BIGINT, BOSS_ID INTEGER, DEPARTMENT_ID INTEGER, ADDRESS_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE PHONE (ID INTEGER NOT NULL, PHONENUMBER VARCHAR, TYPE VARCHAR, EMPLOYEE_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE DEPARTMENT_EMPLOYEE (Department_ID INTEGER NOT NULL, employees_ID INTEGER NOT NULL, PRIMARY KEY (Department_ID, employees_ID))
CREATE TABLE PROJECT_EMPLOYEE (employees_ID INTEGER NOT NULL, projects_ID INTEGER NOT NULL, PRIMARY KEY (employees_ID, projects_ID))
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_DEPARTMENT_ID FOREIGN KEY (DEPARTMENT_ID) REFERENCES DEPARTMENT (ID)
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_ADDRESS_ID FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS (ID)
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_BOSS_ID FOREIGN KEY (BOSS_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE PHONE ADD CONSTRAINT FK_PHONE_EMPLOYEE_ID FOREIGN KEY (EMPLOYEE_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE DEPARTMENT_EMPLOYEE ADD CONSTRAINT FK_DEPARTMENT_EMPLOYEE_employees_ID FOREIGN KEY (employees_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE DEPARTMENT_EMPLOYEE ADD CONSTRAINT FK_DEPARTMENT_EMPLOYEE_Department_ID FOREIGN KEY (Department_ID) REFERENCES DEPARTMENT (ID)
ALTER TABLE PROJECT_EMPLOYEE ADD CONSTRAINT FK_PROJECT_EMPLOYEE_projects_ID FOREIGN KEY (projects_ID) REFERENCES PROJECT (ID)
ALTER TABLE PROJECT_EMPLOYEE ADD CONSTRAINT FK_PROJECT_EMPLOYEE_employees_ID FOREIGN KEY (employees_ID) REFERENCES EMPLOYEE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
